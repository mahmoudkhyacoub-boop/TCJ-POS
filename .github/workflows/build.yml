name: Build Trend Center Jordan POS V114 (Financial Policy Edition)
on: [push]
jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install customtkinter pillow pywin32 pandas openpyxl arabic-reshaper python-bidi
      - name: Build with PyInstaller
        run: |
          pip install pyinstaller
          pyinstaller --noconfirm --onefile --windowed --add-data "icon.png;." --icon "icon.ico" --name "Trend_Center_Jordan_V114" main.py
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Trend_Center_Jordan_V114_EXE
          path: dist/Trend_Center_Jordan_V114.exe
